import React from "react";
import MoreContent from "./MoreContent";

/**
 * More Component
 * Renders the MoreContent component.
 *
 * @returns {React.JSX.Element} The rendered MoreContent component.
 */
const More = () => {
  return <MoreContent />;
};

/**
 * More component propTypes
 */
More.propTypes = {};

export default More;
