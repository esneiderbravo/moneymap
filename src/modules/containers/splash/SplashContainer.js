import React from "react";
import Splash from "../../components/splash/Splash";

/**
 * SplashContainer Component
 *
 * This container component serves as a wrapper for the `Splash` component.
 * It is responsible for rendering the splash screen before authentication.
 *
 * @returns {React.JSX.Element} The rendered Splash component.
 */
const SplashContainer = () => {
  return <Splash />;
};

export default SplashContainer;
