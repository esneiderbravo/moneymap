import React from "react";
import More from "../../components/more/More";

/**
 * MoreContainer Component
 * Serves as a container for the `More` component, enabling better separation of concerns.
 *
 * @returns {React.JSX.Element} The `More` component wrapped in a container.
 */
const MoreContainer = () => {
  return <More />;
};

export default MoreContainer;
